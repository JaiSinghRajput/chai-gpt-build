export interface FormatJsonInput {
  json: string;
  indent?: number;
}

export interface MinifyJsonInput {
  json: string;
}

export interface ValidateJsonInput {
  json: string;
}

export interface RepairJsonInput {
  json: string;
}